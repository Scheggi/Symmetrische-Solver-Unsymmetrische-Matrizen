[package]
name = "sym_solver"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = { version = "0.16.1" }
ndarray-linalg = { version = "0.17.0", features = ["openblas-static"] }

[dev-dependencies]
criterion = "0.7.0"

[[bench]]
name = "cg_benchmark"
harness = false
